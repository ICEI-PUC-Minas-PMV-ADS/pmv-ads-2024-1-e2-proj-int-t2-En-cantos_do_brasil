@model IEnumerable<Encantos_do_Brasil.Models.TelefoneHotel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hotel.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTelefone">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTelefone">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTelefone">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
